// import createContext from "../CustomContext";   you don't need this becuase react provide us createContext method
import { createContext } from "react";
const CourseContext = createContext(); // let the value to be null, we are not passing any value
export default CourseContext;